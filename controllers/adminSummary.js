const express = require('express');
const router = express.Router();
const connection = require('../models/database.js');


router.post('/adminsummary', (req, res) =>{

    const query = 'SELECT * FORM payment'

})